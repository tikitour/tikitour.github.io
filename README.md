# TikiTour Landing (Next.js + Tailwind)

Quick scaffold for a tour company landing page using Next.js and Tailwind CSS.

Install dependencies and run locally:

```bash
npm install
npm run dev
```

Open http://localhost:3000 after `npm run dev`.
